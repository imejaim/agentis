# Agentis — 기획서 (v0.3)

> 작성: 코부장 / 2026-05-13. 대표님 2차 피드백 + GitHub 셋업 반영.
> 진행 중 문서 — "조금씩 더 살펴보면서 추가하고 다듬자" 기조. 끝의 §9(대표님 확인 필요) 봐주세요.
> 코드 레퍼런스: `reference/` 폴더에 원본 레포 클론됨(깃이그노어). 필요 시 코드 레벨로 분석·재조합.

---

## 0. 한 줄 요약

**Agentis** — 사내 **VS Code + cline SR** 환경에서, **워크스페이스 룰 파일 하나**로 부팅되는 **업무처리 에이전트 키트**.
사용자가 에이전트 이름을 짓고 목적을 정하면 → clarify 인터뷰로 시작 → 일하면서 지식이 **위키·그래프("두뇌")** 로 쌓이고 → 에이전트가 **스스로 스킬을 만들며 진화** → **사용자도 "에이전틱 업무처리"를 체득하며 함께 성장**.
회사에서 그냥 **다운받아 복붙**하면 시작되도록. 모토: **카오스 → 유니버스.**

---

## 1. 목적과 성격 (확정)

- ❌ 코딩 에이전트 아님 / ❌ 개인 비서 아님 → ✅ **업무처리 에이전트** (사내 업무를 에이전틱하게 처리)
- ✅ **사내 배포** — 내 GitHub 레포에 올려두고 회사에서 클론/다운로드해서 사용
- ✅ **진입점 = cline SR 워크스페이스 룰** (`.clinerules`)
- ✅ **교육 목적 겸함** — "에이전틱 업무처리란 이런 거구나"를 동료들이 직접 경험
- ✅ **공진화(co-evolution)** — 매 상호작용이 (a) 사용자의 에이전틱 역량 (b) 에이전트의 지식·스킬, 둘 다를 키운다
- ✅ **셋팅 최소화** — 두 진입 경로:
  - **경로 A**: 우리가 만든 폴더를 사용자 프로젝트에 복붙 → 클라인에게 "이 폴더 보고 기본 셋팅해줘"
  - **경로 B (최소)**: cline SR 워크스페이스 룰에 **파일 1개** + **"안녕"** → 알아서 진행
- ✅ **단순하게 간다** — 사내는 프록시 인증서·내부망 제약이 있어 빌드/설치 복잡한 건 부담. 다만 cline SR에게 "이거 설치해줘" 하면 설치는 됨 → **설치 의존은 최소화하되, 필요하면 클라인에게 위임**.

---

## 2. 실행 환경 — cline SR (확인된 사실)

- **cline SR** = cline 오픈소스를 기반으로 **사내에서 쓰도록 만든 사내용 VS Code 익스텐션**. (좀 오래된 베이스일 수 있음.)
- **디폴트 환경 = VS Code + cline SR.**
- 첫 인사("안녕")하면 *"저는 사내 모델 어쩌구..."* 라고 자기소개함 → 모델/시스템 프롬프트는 사내 커스텀.
- 기능 현황(대표님 관찰): **MCP 됨.** 룰·워크플로우·hook·skill 은 **메뉴는 다 있는데 동작은 제한적**인 듯.
  → **Phase 0 과제**: 룰/워크플로우/hook/skill 각각 "어디까지 되는지"를 실제로 찔러봐서 확정. The Seed 설계는 **가장 확실히 되는 것(워크스페이스 룰)** 위에 짓고, 나머지(워크플로우/hook/skill)는 "되면 좋은" 보강으로.

---

## 3. "Agentis" — 컨셉

**Agentis = 에이전트가 진화해 나가는 환경/세계의 이름.** (혼돈 → 질서 잡힌 우주.)

첫 만남(부팅) 시 에이전트가 하는 일:
1. 사용자에게 **에이전트 이름을 짓게** 한다 ("저는 아직 이름이 없어요.")
2. **명확한 목적**을 함께 정한다 — superpowers식 **clarify 인터뷰** (목적·범위·산출물·제약 몇 질문)
3. **사용법을 안내**한다 (어떻게 일을 시키는지 / 어떻게 기억이 쌓이는지 / 어떻게 같이 크는지)

그 후 에이전트는:
- 세션이 바뀌어도 **그 프로젝트를 다 기억·이해**한다 (Hermes 모델)
- 배운 것을 **위키 + 그래프("두뇌")** 에 누적한다 (Karpathy LLM-위키 + graphify)
- 반복되는 일은 **스스로 스킬로 만들고 개선**한다 (Hermes 학습 루프)

---

## 4. 레퍼런스 → 왜 가져왔나 (대표님 의도) + 코드 위치

| 레퍼런스 | 정체 | 가져온 이유 | `reference/` 위치 |
|---|---|---|---|
| `cline/cline` (+ 사내 **cline SR**) | 오픈소스 VS Code AI 에이전트. `.clinerules`·워크플로우·MCP·Plan/Act. | **호스트 플랫폼.** cline SR의 베이스. 워크스페이스 룰을 진입점으로. | `reference/cline/` |
| `obra/superpowers` | 스킬 기반 개발 방법론(브레인스토밍→플랜→실행→TDD→리뷰→완료). Claude Code 플러그인 + 멀티플랫폼. | **첫 인터뷰의 clarify 과정이 좋아 보여서.** 그 인터뷰/명확화 흐름을 차용 (전체 방법론 아님). `skills/`·`docs/` 볼 것. | `reference/superpowers/` |
| `nousresearch/hermes-agent` | 자기개선 에이전트. 스킬 자동 생성·개선, 기억 nudge, 과거 대화 검색, 세션 넘는 사용자 모델. | ① **세션이 달라져도 그 프로젝트를 다 기억·이해**하도록 ② **스스로 스킬을 만들고 진화**하는 기술 적용. | `reference/hermes-agent/` |
| `graphify.net` (= `safishamsi/graphify`) | 코드+문서+이미지 → Tree-sitter+NetworkX+Leiden 지식 그래프. 무중계, 사용자 기존 API 키, 토큰 71배 절감. | **쌓이는 지식이 시각화·그래프화되어 "두뇌처럼" 작동**하도록. `ARCHITECTURE.md` 볼 것. | `reference/graphify/` |
| gist(karpathy) **LLM Wiki** | RAG 대신 LLM이 직접 유지보수하는 마크다운 지식베이스. 3계층(원본/위키/스키마). ingest·query·lint. | **두뇌(지식베이스)를 만들기 위함.** 카오스 → 유니버스. (참고: OMC `/wiki`가 이 패턴의 동작 구현체 — 차용 가능) | `reference/karpathy-llm-wiki/llm-wiki.md` |

**합치면:** cline SR(룰 진입점) 위에서 → superpowers식 인터뷰로 시작 → Hermes식으로 기억하고 진화 → Karpathy 위키 + graphify 그래프로 "두뇌"를 키운다. 그 진화 환경이 **Agentis**.

---

## 5. 우리가 실제로 만들 산출물 (= GitHub 레포 내용물)

1. **The Seed — Agentis 커널** : `.clinerules` 호환 부트스트랩 문서 **1개**.
   - "안녕" → ① clarify 인터뷰(이름·목적) ② 에이전트 정체성 파일 + 메모리 구조 자가 생성 ③ 사용법 안내.
   - **이 파일 하나만으로 끝까지 동작**해야 함(경로 B). 나머지는 보강.
2. **The Kit — 템플릿 폴더** (경로 A) : 스킬 라이브러리(소수 큐레이션), 메모리 스캐폴드(LLM-위키 인덱스 구조), 워크플로우 템플릿, 그래프 시각화용 정적 HTML 뷰어(아래 §7). 사용자가 복붙 + "이거 보고 셋팅해줘".
3. **Docs** : `README.md`(설치/사용 3분컷), Agentis 컨셉 설명, 사내 도입 안내(비개발 직무도 읽을 톤), 에이전트 예시 카탈로그(규격인증 문서 자동화 / 개발 디버깅 / …).

> 레포 구조(안): `seed/` (커널 파일), `kit/` (템플릿 폴더), `docs/`, `examples/`, `PLAN.md`, `README.md`. `reference/`는 깃이그노어.

---

## 6. 동작 시나리오 (해피 패스)

1. 동료가 자기 작업 폴더에 Agentis `kit/`을 복붙(또는 룰 파일 1개만).
2. cline SR 워크스페이스 룰이 커널을 읽음. 동료가 **"안녕"**.
3. 에이전트: *"안녕하세요. 저는 사내 모델 기반인데, 아직 이름이 없어요. 이름을 지어주세요. 그리고 어떤 업무를 도울까요?"* → **clarify 인터뷰**.
4. 에이전트가 **정체성 파일**(이름·목적·범위·말투) + **메모리 폴더**(LLM-위키 `index.md` 등)를 자가 생성하고 인터뷰 결과 기록.
5. **사용법 3줄 안내**.
6. 이후 매 작업: 업무 처리 → 배운 것 위키에 ingest → 그래프 갱신 → (복잡한 작업 후) *"이거 스킬로 만들어 둘까요?"* 제안.
7. **새 세션**: 메모리·그래프를 먼저 읽고 *"지난번 ○○까지 했었죠. 이어서?"* — 잊지 않음.

**첫 사용자 = 우리 파트 동료들.** 만들려는 에이전트 예: ▷ 규격인증 문서 자동화 에이전트 ▷ 개발 디버깅 에이전트 ▷ 그 외 다양 → 인터뷰 질문지·예시 카탈로그를 이 폭에 맞춰 설계.

---

## 7. 지식 그래프 / 시각화 ("두뇌")

참조: `brunch.co.kr/@abrahamsong/164` (graphify+LLM-위키 실사용기), facebook 이미지(대표님 공유 — 아직 못 봄, §9-1).

- **모델**: 노드=원자 단위 개념, 엣지=관계. AST는 결정론적 추출, 문서는 LLM이 의미 관계 추출.
- **시각화 형태**: D3.js **force network map** (정적 HTML 한 장이면 cline SR 안 webview/브라우저로 띄우기 쉬움 → 빌드 의존 없음).
- **부가 기능(graphify 차용)**: Leiden 커뮤니티 클러스터링 / "신(神) 노드" = 평균 차수 + 2σ 이상 중심 개념 자동 강조 / 신뢰도 표시(EXTRACTED 100% · INFERRED 점수 · AMBIGUOUS).
- **단순화 방침**: 초기엔 graphify 풀스택 임베드 말고, **메모리 위키(.md)에서 노드/엣지를 추출해 JSON 하나 만들고 → 정적 D3 HTML이 그걸 읽어 그림** 정도로. graphify 본체 연동은 Phase 3에서.

---

## 8. 단계 (phased)

- **Phase 0 (지금)** — ① GitHub 레포 만들고 커밋/푸시 시작 ✅(진행 중) ② `reference/` 클론 ✅ ③ **cline SR 제약 실측**(룰/워크플로우/hook/skill/MCP 각각 어디까지 되나).
- **Phase 1 (MVP)** — **The Seed v1**: 부트스트랩 + clarify 인터뷰 + 정체성/메모리 자가 생성 + 사용법 안내. **단일 룰 파일 하나로 끝까지** 동작. + 손으로 예제 에이전트 1개(가능하면 "규격인증 문서 자동화" 류) dogfood.
- **Phase 2** — **The Kit**: 메모리=LLM-위키 운영(ingest/query/lint), 기본 스킬 몇 개, 워크플로우 템플릿, 정적 D3 그래프 뷰어, 경로 A 검증.
- **Phase 3** — **두뇌 강화**: graphify 본체 연동, Hermes식 학습 루프(스킬 자동 생성·개선·nudge), 과거 작업/대화 검색.
- **Phase 4** — **사내 확산**: 온보딩 문서 정리 + 동료별 에이전트 파일럿 + 피드백 반영.

---

## 9. 대표님 확인 필요 ❓

1. **facebook 이미지** — `facebook.com/photo/?fbid=2208312009941348&...` 는 로그인 필요라 제가 못 봤습니다. 그 이미지가 어떤 그래프 모양/스타일인지 한 줄로만 알려주시거나, 캡처해서 이 폴더에 넣어주세요(예: `docs/ref-graph-style.png`).
2. **GitHub 레포** — 아래 §10대로 `imejaim` 계정에 **`agentis`** 이름, **private**으로 만들 예정. 이름/공개여부 이대로 OK? (동료들이 받아야 하니 나중에 public 전환하거나 협업자 추가하면 됨.)
3. **cline SR 실측** — 제가 코드(`reference/cline/`)만으로는 "사내 커스텀 부분"을 못 봅니다. Phase 0에서 대표님이 cline SR 켜고 룰/워크플로우/hook/skill 한 번씩 찔러본 결과를 알려주시면 The Seed를 거기 맞춥니다. (아니면 일단 "워크스페이스 룰만 확실히 된다" 가정으로 진행.)

---

## 10. 코부장 다음 행동

1. ✅ `git init`, `.gitignore`(reference/ 제외), `reference/` 5개 클론.
2. ⏳ GitHub 레포 `imejaim/agentis` (private) 생성 → 첫 커밋 푸시.
3. The Seed v1 초안 작성 (단일 `.clinerules` 부트스트랩 — 인터뷰·자가셋업·안내 포함).
4. clarify 인터뷰 스크립트 설계 — `reference/superpowers/skills/` 의 brainstorming 스킬 코드 레벨 분석 후 업무 에이전트용 각색.
5. 메모리(LLM-위키) 폴더 구조 v1 — `reference/karpathy-llm-wiki/llm-wiki.md` + OMC `/wiki` 구현 참고.
6. 정적 D3 그래프 뷰어 PoC — 메모리 .md → 노드/엣지 JSON → HTML 한 장.

---

*— 코부장. §9-2(레포 이름/공개여부)만 멈칫하면 알려주시고, 아니면 그대로 푸시하고 The Seed 들어갑니다.*
