---
name: {{업무이름}}
inputs: {{어떤 입력 — 형식, 위치}}
outputs: {{어떤 산출물 — 형식, 위치}}
script: {{<업무이름>.py 또는 (없음)}}
created: {{YYYY-MM-DD}}
updated: {{YYYY-MM-DD}}
---

# {{업무이름}} 워크플로우

## 목적
{{이 워크플로우가 처리하는 업무 한 줄.}}

## 절차
1. **입력 확인** — {{어디서 무엇을 받는가. 누락/형식 이상 먼저 체크.}}
2. **처리** — {{단계들. 코드로 할 수 있는 건 `python agentis/workflows/{{업무이름}}.py --in ... --out ...` 로.}}
3. **검증** — {{출력이 기대대로인지 확인. 예: 처리 항목 수 == 입력 항목 수, 샘플 N건 원본 대조, 합계 일치 등. 사내 도구 — 검증 안 된 건 "확인 필요"로 보고.}}
4. **산출물 전달** — {{무엇을 어디에. 사용자에게 결과 + (있으면) 스크립트 + 검증 결과 + "다시 돌리는 법" 한 줄.}}
5. **기억 갱신** — `memory/log.md` 한 줄, 새 개념/엔티티/소스 페이지 반영, `hot.md` 갱신, `python agentis/graph/build_graph.py`.

## 주의 / 엣지 케이스
{{- 자주 틀리는 지점}}
{{- 사람이 꼭 확인해야 하는 것}}

## 관련
- 메모리: [[concepts/...]], [[entities/...]]
- 스킬: ../skills/...
