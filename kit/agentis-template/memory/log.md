# 로그

작업 연대기입니다. 새 항목은 **맨 위**에 추가하세요. 형식:

```
## [YYYY-MM-DD] <type> | <제목>
- 한 줄 요약 / 결과 / 관련 페이지 [[..]]
```

type: `setup` | `task` | `ingest` | `skill` | `lint` | `note`

> 팁: 각 항목이 `## [` 로 시작하므로 `grep "^## \[" log.md` 로 타임라인을 뽑을 수 있습니다.

---

## [{{YYYY-MM-DD}}] setup | 에이전트 부팅
- 이름·목적·범위 인터뷰 완료. `agentis/` 구조 생성. (`agent.md`, `memory/*`, `skills/`, `workflows/`, `graph/`)
